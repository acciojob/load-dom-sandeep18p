document.addEventListener("DOMContentLoaded", function() {
  // When the DOM is loaded, write "DOM load success" on the webpage
  var div = document.createElement("div");
  var c = "DOM load success";
  document.body.appendChild(c);
});
